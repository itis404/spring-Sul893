spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect
  sql:
    init:
      mode: never
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms
      lettuce:
        shutdown-timeout: 100ms

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

external:
  currency:


logging:
  level:
    root: WARN
    com.privetmedved: DEBUG